# Lifecycle Management & Statecharts

Never manually hand-author reactive skill structures. Always use the `skill-manager` skill to CREATE, UPDATE, or MIGRATE skills.

Every reactive skill must maintain a synchronized `STATECHART.md` containing a Mermaid `stateDiagram-v2` visualization matching its `skill.yaml` topology.