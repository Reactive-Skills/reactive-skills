import { GuidePage } from '@/features/guide/GuidePage';

export const metadata = {
  title: 'Guide',
  description: 'Understand Reactive Skills, run your first workflow, and decide how to adopt it.',
};

export default function GuideRoute() {
  return <GuidePage />;
}
