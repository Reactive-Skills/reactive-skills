/** @type {{ initial: string, nodes: import('@/contracts/types').StateNode[] }} */
export const stateMachine = {
  initial: 'EXPLORE',
  nodes: [
    {
      name: 'EXPLORE',
      description: 'Read the relevant prompt slice and gather just enough context before committing to a plan.',
      guard: 'context.sufficient',
      transitionsTo: 'PLAN',
      emits: 'ContextGathered',
    },
    {
      name: 'PLAN',
      description: 'Draft an ordered plan and expose it as a live deliverable so a human or parent skill can review it.',
      guard: 'plan.approved',
      transitionsTo: 'EXECUTE',
      emits: 'PlanProposed',
    },
    {
      name: 'EXECUTE',
      description: 'Perform the work in small steps. Each meaningful change appends one immutable event.',
      guard: 'artifacts.present',
      transitionsTo: 'VERIFY',
      emits: 'ArtifactProduced',
    },
    {
      name: 'VERIFY',
      description: 'Check the result against the guard. If it fails, the run reports the reason instead of pretending success.',
      guard: 'checks.passed',
      transitionsTo: 'DONE',
      emits: 'ChecksPassed',
    },
    {
      name: 'DONE',
      description: 'Emit the final deliverable and seal the event log so the run stays fully reconstructable.',
      guard: null,
      transitionsTo: null,
      emits: 'SkillCompleted',
    },
  ],
};

export const hierarchicalStateMachine = {
  name: 'tdd-refactor',
  initial: 'RED_SPEC',
  description: 'Hierarchical Statechart (HSM) with nested composite states, automatic initial substate resolution, and ancestor event bubbling.',
  steps: [
    {
      id: 'RED_SPEC',
      name: 'RED_SPEC',
      type: 'atomic',
      depth: 1,
      path: ['RED_SPEC'],
      description: 'Author a failing test in tests/calc.test.ts establishing a concrete, verifiable specification boundary.',
      guard: 'event.payload.exit_code != 0',
      transitionsTo: 'GREEN_CODE',
      emits: 'TEST_RAN (fail)',
      promptTemplate: 'states/red_spec.md',
    },
    {
      id: 'GREEN_CODE',
      name: 'GREEN_CODE',
      type: 'atomic',
      depth: 1,
      path: ['GREEN_CODE'],
      description: 'Write minimal implementation in src/calc.ts until tests transition from failing to passing (exit code 0).',
      guard: 'event.payload.exit_code === 0',
      transitionsTo: 'REFACTOR.CLEAN_CODE',
      emits: 'TEST_RAN (pass)',
      promptTemplate: 'states/green_code.md',
    },
    {
      id: 'REFACTOR.CLEAN_CODE',
      name: 'CLEAN_CODE',
      parent: 'REFACTOR',
      type: 'substate',
      depth: 2,
      path: ['REFACTOR', 'CLEAN_CODE'],
      description: 'Substate 1 of REFACTOR: Simplify logic, eliminate duplication, and extract helpers without changing observable behavior.',
      guard: 'clean_passes >= 1',
      transitionsTo: 'REFACTOR.PERF_AUDIT',
      emits: 'CLEANING_DONE',
      promptTemplate: 'states/clean_code.md',
    },
    {
      id: 'REFACTOR.PERF_AUDIT',
      name: 'PERF_AUDIT',
      parent: 'REFACTOR',
      type: 'substate',
      depth: 2,
      path: ['REFACTOR', 'PERF_AUDIT'],
      description: 'Substate 2 of REFACTOR: Optimize memory allocations and latency. Unhandled failures bubble directly up to the parent REFACTOR state!',
      guard: 'audit_score >= 90',
      transitionsTo: 'AUDIT_VERIFY',
      emits: 'AUDIT_PASSED',
      promptTemplate: 'states/perf_audit.md',
      canBubble: true,
    },
    {
      id: 'AUDIT_VERIFY',
      name: 'AUDIT_VERIFY',
      type: 'atomic',
      depth: 1,
      path: ['AUDIT_VERIFY'],
      description: 'Verify all project-wide integration tests and static analysis gates pass before delivering.',
      guard: 'event.payload.exit_code === 0',
      transitionsTo: 'COMPLETED',
      emits: 'ALL_CHECKS_PASSED',
      promptTemplate: 'states/audit_verify.md',
    },
    {
      id: 'COMPLETED',
      name: 'COMPLETED',
      type: 'terminal',
      depth: 1,
      path: ['COMPLETED'],
      description: 'Final deliverable projections rendered from templates/summary.md.hbs; execution ledger is sealed.',
      guard: null,
      transitionsTo: null,
      emits: 'DELIVERABLE_READY',
      promptTemplate: 'states/completed.md',
    },
  ],
  compositeStates: [
    {
      id: 'REFACTOR',
      name: 'REFACTOR',
      description: 'Composite state managing the iterative cleanup and performance verification cycle.',
      initialSubstate: 'CLEAN_CODE',
      onEnter: 'emit REFACTOR_CYCLE_STARTED',
      onExit: 'emit REFACTOR_CYCLE_CONCLUDED',
      substateIds: ['REFACTOR.CLEAN_CODE', 'REFACTOR.PERF_AUDIT'],
      ancestorTransitions: [
        {
          signal: 'TEST_RAN (fail)',
          guard: 'event.payload.exit_code != 0',
          target: 'GREEN_CODE',
          note: 'Catches bubbled test regressions from any substate and rolls back to GREEN_CODE.',
        },
        {
          signal: 'GLOBAL_ABORT',
          guard: 'true',
          target: 'RED_SPEC',
          note: 'Ancestor abort policy: cancels active substate and resets to spec phase without substate boilerplate.',
        },
      ],
    },
  ],
};

